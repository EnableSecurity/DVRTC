[general]

[logfiles]
; Log to Docker console only (stdout/stderr). Include verbose output so the
; dialplan's Verbose() traces are visible in container logs during exercises.
console => notice,warning,error,dtmf,verbose

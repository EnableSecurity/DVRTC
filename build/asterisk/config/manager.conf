[general]
enabled = yes
bindaddr = 127.0.0.1
port = 5038
; Local-only AMI account used by the testing container to query active calls.
; This intentionally simple credential is only reachable over loopback.
[dvrtc]
secret = dvrtc
read = call,command
write = command
deny = 0.0.0.0/0.0.0.0
permit = 127.0.0.1/255.255.255.255
